{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities",
  "windows": ["main", "voice-pill", "export_*"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "global-shortcut:default",
    "updater:default",
    "process:default",
    "dialog:allow-save",
    "shell:allow-open",
    "deep-link:default",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/coagent-server",
          "sidecar": true
        }
      ]
    },
    {
      "identifier": "shell:allow-spawn",
      "allow": [
        {
          "name": "binaries/coagent-server",
          "sidecar": true
        }
      ]
    }
  ]
}
